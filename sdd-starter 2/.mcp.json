{
  "_note": "Scope MCP per repo. Every connected server's tool definitions are resent on every turn — the bill scales with the menu, not with what you call. Add only what THIS repo needs.",
  "mcpServers": {}
}
